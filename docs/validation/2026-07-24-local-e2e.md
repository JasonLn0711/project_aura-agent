# VOISS AURA Control Room — 2026-07-25 Final-source Local E2E Validation

Product milestone: `LOCAL_E2E_VALIDATED`
Live-run status: `LIVE_MINIMUM_COMPLETED`
Evidence date: 2026-07-25 (Asia/Taipei); original milestone initiated 2026-07-24

## Validation scope

This record covers one controlled Web → AURA Bridge → official Codex
app-server flow on a synthetic AURA/Git fixture. The run demonstrates the P0
local operating path: source-backed claim review, read-only planning, explicit
`allow_once`, isolated workspace write, retained validation, trusted diff,
evidence export, and a hash-chained audit trail.

The evidence supports a local P0 candidate on the verified host. Additional
repositories, host distributions, concurrent workloads, and remote delivery
authority enter through separate activation and validation paths.

## Release identity

| Field | Value |
|---|---|
| Source repository baseline | `6807f516d1083051d75373f110ac871f677f75ce` |
| Working branch | `feat/voiss-aura-control-room` |
| Working source state | uncommitted feature worktree rooted at the pinned baseline |
| Fixture base commit | `b5cd87ec6fa9a745398956c31b4967ebf72e5ee2` |
| Node.js | `v22.23.1` |
| pnpm | `11.17.0` |
| Python environment | `3.11.15` through `uv` |
| uv | `0.11.6` |
| Git | `2.43.0` |
| Codex CLI | `0.145.0` |
| Podman | `4.9.3` |
| Host | Ubuntu 24.04 lineage, Linux `7.0.0-28-generic`, x86_64 |

Official `codex login status` reported `Logged in using ChatGPT`. The retained
status and evidence surfaces expose account readiness without email,
credential, or token material.

## Live workflow evidence

| Evidence field | Retained value |
|---|---|
| Correlation ID | `corr-e0d2af0c338132daf5ccb9bdd022353a` |
| CopilotKit read-only invocation | `1efe3e1a-d061-42bd-a506-2b513bed6ab6` |
| CopilotKit write-activation invocation | `8817108d-9c94-4c50-9184-0ac40c5d3816` |
| CopilotKit approval-resume invocation | `523134ad-b566-43d6-84b1-3888465ca81a` |
| Codex Bridge read-only run | `31a44d44-e20a-487f-bb94-e1d223fd5e82` |
| Codex Bridge write/evidence run | `209611ef-bc40-4f00-863e-e3045788ada0` |
| Activation approval | `f91cd69f-1e88-4f9e-8832-875312b1ed53` |
| Approval decision | `write_activation / allow_once / isolated_workspace_write` |
| Requested and observed model | `gpt-5.6-sol` |
| Observed reasoning effort | `max` |
| Observed sandbox | `workspaceWrite` through official `managed-bubblewrap` |
| Run policy | `networkAccess=false` |
| Persisted run events | 1,139 across the completed live workflow |
| Codex events in the exported write-run packet | 366 |
| Patch | `queue.Queue()` → `queue.Queue(maxsize=8)` |
| Validation gate | 2 passed, 0 failed, 0 stale, 0 overflow |
| Audit chain | valid; 1 bootstrap event plus 10 correlation-scoped workflow events |

The two retained validation commands ran against the terminal patch:

1. `/bin/bash -lc 'python3 -m pytest -q'` — exit `0`
2. `/bin/bash -lc 'git diff --check'` — exit `0`

Both checks match mutation generation `6` and frozen patch SHA-256
`827b843061eebc62619df0a8b91707673c7dc860a56aa9404c535e36efb81ab6`.

The CopilotKit runner retained three parent-linked invocations: the
write-activation invocation names the read-only invocation as its parent, and
the approval-resume invocation names the interrupted write-activation
invocation as its parent. The settled runner state records
`is_running=0` and `current_run_id=null`. This proves that resume preserves the
thread and evidence lineage while allocating a new run ID for the new
invocation.

Approval timeout (`timed_out/paused`, replay, resume, and stop) and
stale-running／normal-close／app-server-crash lifecycle reconciliation are
contract-test evidence. The guarded live run proves the successful
`allow_once` path; automatic active/write recovery and the remaining approval
decisions retain their target-host live-trace activation gates.

## Runtime validity

- Rootless Podman + official Codex `0.145.0` + nested
  `managed-bubblewrap`: `valid_target_runtime`.
- Deterministic demo fixtures: `valid_baseline`; every simulated event keeps
  its demo classification.
- Direct-host `:workspace` on this host: `blocked_runtime` until the targeted
  AppArmor prerequisite is activated; the supported rootless Podman lane
  completed the target run.

The runtime image was rebuilt from the digest-pinned
`ubuntu:24.04@sha256:c4a8d550...` base:

- image ID:
  `9c379b112906ca025141698d28e5ac166c7839f5ccee281bd29661f58a32a157`
- image digest:
  `sha256:5f442b726a16550240a8ab38898632fa0d127fb52041caa3d7a656e518d27d38`

The policy observation and active network test remain separate evidence
layers. A managed-sandbox socket canary attempted `1.1.1.1:443` and exited
with `PermissionError: [Errno 1] Operation not permitted`. The direct-host
preflight exited with
`bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`, which selects
the documented Podman path on this host.

## Evidence packet

The owner-controlled local export is retained at:

```text
/tmp/voiss-aura-live-final-source7-20260724-hDoyJi/exports/209611ef-bc40-4f00-863e-e3045788ada0
```

All three export files use owner-only mode `0600`. The control-plane and
CopilotKit runner SQLite databases also use `0600`; their parent directories,
export root, and isolated worktree roots use `0700`. The two databases use
independent schema files: `VOISS_DB_PATH` owns control-plane lifecycle metadata,
and `VOISS_AGENT_DB_PATH` owns CopilotKit thread/run history.
After completion, the primary fixture repository remained clean at base commit
`b5cd87ec6fa9a745398956c31b4967ebf72e5ee2`; only the isolated worktree retained
the `src/aura/asr/threads.py` modification.

| Artifact | SHA-256 |
|---|---|
| `changes.patch` | `827b843061eebc62619df0a8b91707673c7dc860a56aa9404c535e36efb81ab6` |
| `evidence.json` | `67f95f03d00ad94e1b27a9e21a83a20ee9ebb2b6062ce04f123a645302d81692` |
| `checksums.sha256` | `f90fc3174b78accb7d14c070216fa44158216bc4311250c2e764fbd459654e6d` |

`sha256sum -c checksums.sha256` returned `OK` for `changes.patch` and
`evidence.json`. The evidence packet records authority as `false` for push,
merge, deploy, and external messages.

## Audit sequence

The fresh validation database contains eleven events:

1. `control_plane.initialized`
2. `claim.confirmation_gate.passed`
3. `claim.review`
4. `action.delegated`
5. `agent.run.requested`
6. `run.plan.completed`
7. `agent.run.requested`
8. `approval.allow_once`
9. `agent.run.requested`
10. `run.validation.completed`
11. `evidence.exported`

Sequences 2–11 share the live correlation ID. This count describes the final
clean validation database rather than cumulative attempts.

## Deterministic demo walkthrough evidence

A coherent headless Playwright walkthrough used `pnpm demo` and completed
Control Room → Sessions → Actions → read-only plan → `allow_once` → completed
diff/tests → Trust → browser export in 3.42 seconds. The downloaded packet is:

```text
/tmp/voiss-aura-demo-walkthrough-20260724.json
```

| Field | Retained value |
|---|---|
| Suggested filename | `corr-demo-voiss-001-evidence.json` |
| Classification | `deterministic_demo_evidence` |
| Correlation | `corr-demo-voiss-001` |
| Run status | `completed` |
| Embedded digest | present; 64 characters |
| Outer file SHA-256 | `bde472bdcb409009d45f26dcb06c8f6f7dbbf925727c9d3d55e2bc88f8148e82` |

This machine walkthrough proves the scripted browser sequence and packet
classification. Human spoken rehearsal, timing under presentation conditions,
and reviewer sign-off remain separate release activation gates.

## Final-source quality matrix

| Command or control | Result |
|---|---|
| `pnpm install --frozen-lockfile` | PASS; all 8 workspace projects resolved from lockfile |
| `uv sync --all-extras --all-packages --frozen` | PASS; 153 packages checked |
| `pnpm format:check` | PASS; Prettier 3.9.6 checked the Web, shared package, Codex Bridge, script, package, workspace, and workflow surfaces |
| `pnpm lint` | PASS |
| `pnpm typecheck` | PASS |
| `pnpm test` | PASS; 116 JS/TS tests plus architecture-boundary check |
| `pnpm --filter @voiss/codex-bridge run check` | PASS; 55 Codex Bridge tests, included in the 116-test workspace total |
| `pnpm test:coverage` | PASS; Web V8 coverage: 65.07% statements, 57.88% branches, 66.79% functions, 65.75% lines |
| `uv run pytest -q services/aura-bridge/tests` | PASS; 14 tests |
| `uv run pytest -q` | PASS; 398 tests, 2 dependency deprecation warnings |
| `uv run ruff check services/aura-bridge` | PASS |
| `uv run ruff format --check services/aura-bridge` | PASS; 4 files already formatted |
| `pnpm test:e2e` | PASS; 18 deterministic scenarios plus 1 disconnected-local browser scenario |
| `VOISS_LIVE_E2E=1 pnpm test:e2e:live` | PASS; 1 guarded real scenario in 2.0 minutes |
| Axe main-screen accessibility scan | PASS inside deterministic E2E case 01 |
| Browser token and path-boundary checks | PASS inside E2E cases 14–15 |
| Repository credential-pattern scan | PASS; one filename candidate, `services/codex-bridge/tests/bridge.test.ts`, was reviewed as a server-side environment-variable reference; no credential literal matched |
| `pnpm demo` and retained browser walkthrough | PASS; coherent scripted flow and browser export completed in 3.42 seconds |
| `pnpm build` | PASS; Next.js production build and 7-route page generation |
| `pnpm codex:runtime:build` | PASS; digest-pinned base and cached local image ID retained above; apt package versions are not individually pinned |
| `.github/workflows/voiss-control-room.yml` | Implemented with frozen install, lint, typecheck, unit/coverage, build, Python, E2E, and architecture gates; remote execution activates with the reviewed target commit |
| Architecture package regeneration and `generate.py --check` | PASS |

Complete stdout and stderr are retained in [`docs/validation/logs/`](logs/):

- [`01-frozen-install.log`](logs/01-frozen-install.log)
- [`02-web-quality.log`](logs/02-web-quality.log)
- [`03-python-quality.log`](logs/03-python-quality.log)
- [`04-browser-e2e.log`](logs/04-browser-e2e.log)
- [`05-security-and-architecture.log`](logs/05-security-and-architecture.log)
- [`06-live-e2e.log`](logs/06-live-e2e.log)
- [`checksums.sha256`](logs/checksums.sha256)

The six logs and checksum manifest use owner-only mode `0600`;
`sha256sum -c docs/validation/logs/checksums.sha256` verifies every retained
log.

The retained 1440 × 1436 demo screenshot is
`screenshots/2026-07-24-control-room-demo.png`, SHA-256
`d2207ad08b24191fc3dc590fe6e20b1cafa0f3c76f4fce7998cbd90aa5d1c2ac`.

## Decision

- Production default: `none`; release authority remains a separate review
  decision.
- Operational P0 path: rootless Podman with official Codex app-server and
  nested managed sandbox.
- Operational fallback: deterministic demo for credential-free rehearsal and
  contract presentation.
- Research candidate: additional real repositories through the same
  allowlist, approval, isolated-worktree, validation, and evidence controls.
- Next optimization candidate: dedicated live traces for deny,
  `allow_run_scope`, stop/recovery, and app-server command/file approval
  callbacks.

Local authority ends at reviewed patch and evidence export. Push, merge, pull
request, deployment, publication, and external messaging remain separately
activated work packages.
